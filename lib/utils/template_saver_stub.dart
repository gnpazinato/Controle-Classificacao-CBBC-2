import 'dart:typed_data';

Future<String?> defaultSaveTemplate(String filename, Uint8List bytes) async {
  throw UnsupportedError(
    'defaultSaveTemplate não implementado para esta plataforma.',
  );
}
